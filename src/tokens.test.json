{
    "MONGO_URI": "mongodb://localhost:27017/myapp",
    "--NOTE--": "If you are changing the port, you have to change database type from local and change PORT in config file from src/client folder",
    "PORT": 27017
}